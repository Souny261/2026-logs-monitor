server:
  http_listen_port: 9080

positions:
  filename: /run/promtail/positions.yaml

clients:
  - url: http://172.30.0.10:3100/loki/api/v1/push

scrape_configs:
  - job_name: local-file-logs
    static_configs:
      - targets:
          - localhost
        labels:
          job: local-file-logs
          __path__: /logs/*.log